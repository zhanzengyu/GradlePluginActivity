pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "GradlePluginActivity"
include ':app'
// 这里需要注释，否则会报错：'buildSrc' cannot be used as a project name as it is a reserved name
// include ':buildSrc'
